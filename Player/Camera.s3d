/* Camera */
#ifndef CAMERA_S3D
#define CAMERA_S3D 

class Camera {
	var pos;
	var angle;
	var view; // boolean
	var cam;
	var offset;
};

function Camera::Camera() {
	pos = [0, 1.3, 0];
	angle = 180;
	view = false;
	cam = CameraGetCurrent();
	offset = 2;
}
#endif