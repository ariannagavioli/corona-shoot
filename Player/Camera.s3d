/* Camera */
#ifndef CAMERA_S3D
#define CAMERA_S3D 

class Camera {
	var position;
	var angle;
	var view; // boolean
	var cam;
	var offset;
};

function Camera::Camera() {
	position = [0, 1.3, 0];
	angle = 180;
	view = false;
	cam = CameraGetCurrent();
	offset = 3;
}
#endif